{
  "name": "autocut-document-scanner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "build": "vite build",
    "test:e2e": "playwright test",
    "train": "python scripts/train_yolo.py",
    "export": "python scripts/export_onnx.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^8.0.12"
  }
}
